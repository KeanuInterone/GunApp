<p id="notice"><%= notice %></p>

<div class="row">
	<div class="col-sm-10">
		<h1><%= @brand.name %></h1>
	</div>
	<div class="col-sm-2">
		<%= link_to 'Edit', edit_brand_path(@brand)%>
	</div>
</div>

Liked by:
<% @brand.favored_by.each do |u| %>
	<%= link_to u.email, u %>
<% end %>


<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Image</th>
      <th>Caliber</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @brand.guns.each do |gun| %>

      <tr>
        <td><%= gun.name %></td>
        <td><%= gun_image(gun)%></td>
        <td><%= gun.caliber %></td>
        <td><%= link_to 'Show', gun %></td>
      </tr>
    <% end %>
  </tbody>
</table>

